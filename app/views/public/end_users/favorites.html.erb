<p>いいねした投稿一覧</p>
<%= link_to image_tag( @post.post.variant(resize: '100x100') ), post_path(post.id) %>
<%= link_to post.end_user.name, end_user_path(post.end_user.id) %>
<i class="fa fa-heart unlike-btn"></i>
<%= post.favorites.length %>
<%= link_to post.title, post_path(post.id) %>